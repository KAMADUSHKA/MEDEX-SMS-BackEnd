const BadRequestError = require("./errors/bad-request-error");
const CustomError = require("./errors/custom-error");
const DatabaseConnectionError = require("./errors/database-connection");
const NotFoundError = require("./errors/not-found-error");
const RequestValidationError = require("./errors/request-validation-error");

const checkAuth = require("./middleware/check-auth");
const errorHandler = require("./middleware/error-handler");
const validateRequest = require("./middleware/RequestFilter/validate-request");
const config = require("./config/config");
const listener = require("./NATS/CommonListener");
const publisher = require("./NATS/CommonPublisher");

module.exports = {
  BadRequestError,
  CustomError,
  DatabaseConnectionError,
  NotFoundError,
  RequestValidationError,
  checkAuth,
  errorHandler,
  validateRequest,
  config,
  listener,
  publisher,
};
