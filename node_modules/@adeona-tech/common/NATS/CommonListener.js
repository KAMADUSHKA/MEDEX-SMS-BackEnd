const nats = require("node-nats-streaming");
const Listener = require("./Listener_base");
class CommonListener extends Listener {
  onMessage(parsedData, msg) {
    console.log("Event Data!", parsedData);
    //manual ack
    msg.ack();
  }
}
module.exports = CommonListener;
